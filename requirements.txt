# ClipForge Python dependencies (system deps: ffmpeg + Ollama, installed separately)
faster-whisper>=1.0.0
streamlit>=1.32

# Web UI server (server.py)
starlette>=0.37
uvicorn>=0.29
python-multipart>=0.0.9

# Campaign rules-doc extraction
pypdf>=4.0
python-docx>=1.1

# Speaker-follow 9:16 / square crop
mediapipe>=0.10.9
opencv-python-headless>=4.8

# Clip-boundary silence trim
silero-vad>=5.1

# Web & YouTube video downloading
yt-dlp>=2024.0.0

# NVIDIA GPU acceleration for faster-whisper on Windows (CUDA 12)
nvidia-cublas-cu12
nvidia-cudnn-cu12
nvidia-cuda-nvrtc-cu12
